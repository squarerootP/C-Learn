#include <iostream>

int main(){

    //for each loop = loop that eases the traversal
    // over an iterable data set
    // i think this is equal to for i in (iterable)
    char grades[] = {'a','b', 'c', 'd', 'e'};
    for(char grade : grades){
        std:: cout << grade << "\n";
    }
    // for (dtype element : iterable){}



}